{
  "name": "anming-vpn-client-vue",
  "version": "0.1.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"electron .\"",
    "build:renderer": "vite build",
    "start": "electron .",
    "build": "npm run build:renderer && electron-builder"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^8.0.0",
    "electron": "^26.0.0",
    "electron-builder": "^24.6.0",
    "vite": "^5.0.0",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "vue": "^3.3.4"
  },
  "build": {
    "appId": "tech.anming",
    "files": [
      "dist/**",
      "electron/**",
      "src/renderer/**",
      "package.json"
    ]
  }
}
